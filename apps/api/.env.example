DATABASE_URL=postgresql://fastpay:fastpay@localhost:5432/fastpay
REDIS_URL=redis://localhost:6379
PORT=4000
# AES-256-GCM key for venue payment credentials at rest (32 bytes, base64).
# This is a throwaway dev-only key — generate your own per environment with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
PAYMENT_CREDENTIALS_ENCRYPTION_KEY=yytvrXOfvVCcySNeu3aR7Y8NtKM9nbAEiF0VCQWCIc4=
# HMAC pepper for guest phone hashing (loyalty opt-in) — throwaway dev value, any non-empty string.
GUEST_PHONE_HASH_PEPPER=dev-only-pepper-change-me
