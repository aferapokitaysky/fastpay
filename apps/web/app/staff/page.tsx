import Link from "next/link";
const tables = [{ id: "01", state: "Вільний", className: "" }, { id: "02", state: "1 240,00 ₴", className: "busy" }, { id: "03", state: "680,00 ₴", className: "busy" }, { id: "04", state: "Оплачується", className: "paying" }, { id: "05", state: "Сплачено", className: "" }, { id: "06", state: "Вільний", className: "" }];
export default function StaffPage() { return <main className="shell"><header className="top"><span className="brand"><b>F</b>fastpay</span><span className="demo">ЗМІНА · АННА</span></header><section className="screen"><p className="eyebrow">ОСНОВНИЙ ЗАЛ</p><div className="floor-title"><h1>Зал</h1><button className="primary" aria-label="Додати стіл">＋</button></div><div className="floor-grid">{tables.map((table) => <Link key={table.id} className={`table-card ${table.className}`} href="/t/demo-table-02"><strong>{table.id}</strong><small>{table.state}</small>{table.className === "busy" && <em>● Зайнятий · 28 хв</em>}{table.className === "paying" && <em>◌ Очікуємо оплату</em>}</Link>)}</div></section></main>; }
