{
  "name": "@fastpay/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod contracts (DTOs, error envelope) for FastPay web <-> api.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests in @fastpay/contracts yet\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
